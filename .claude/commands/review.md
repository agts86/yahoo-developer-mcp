変更されたファイルを `.claude/rules/` の規約に照らしてコードレビューしてください。

確認観点:
1. **アーキテクチャ層の依存方向** — 層を跨いだ不正な依存がないか（`.claude/rules/architecture.md`）
2. **型安全性** — `any`使用・戻り値型省略・`@ts-ignore`がないか
3. **複雑度** — 循環的複雑度10超・ネスト3階層超・50行超のメソッドがないか
4. **インポートスタイル** — 型インポートに`import type`を使っているか、拡張子が`.js`か
5. **エラーハンドリング** — `console.log`使用・エラーを握りつぶしていないか

問題があれば具体的なファイル名・行番号とともに指摘し、修正案を提示してください。
